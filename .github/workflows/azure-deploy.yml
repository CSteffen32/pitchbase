# GitHub Actions deployment disabled - using Azure Deployment Center instead
# To enable: Uncomment the workflow below and set up GitHub Secrets
# To use Azure Deployment Center: Go to Azure Portal → Web App → Deployment Center

# name: Deploy to Azure
#
# on:
#   push:
#     branches:
#       - main
#       - master
#   workflow_dispatch:
#
# env:
#   AZURE_WEBAPP_NAME: pitchbase-app
#   NODE_VERSION: '20.x'
#
# jobs:
#   build-and-deploy:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#     
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4
#
#       - name: Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: ${{ env.NODE_VERSION }}
#           cache: 'npm'
#
#       - name: Install dependencies
#         run: |
#           npm ci
#           npm run db:generate
#
#       - name: Build Next.js app
#         env:
#           DATABASE_URL: ${{ secrets.DATABASE_URL }}
#           DIRECT_URL: ${{ secrets.DIRECT_URL }}
#         run: |
#           npm run build
#
#       - name: Create deployment package
#         run: |
#           zip -r deploy.zip . \
#             -x "node_modules/*" \
#             -x ".next/*" \
#             -x ".git/*" \
#             -x "*.zip" \
#             -x ".github/*" \
#             -x "azure-deployment-guide.md" \
#             -x "deploy-to-azure.sh"
#
#       - name: Deploy to Azure Web App
#         uses: azure/webapps-deploy@v2
#         with:
#           app-name: ${{ env.AZURE_WEBAPP_NAME }}
#           publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
#           package: ./deploy.zip
#
#       - name: Run database migrations
#         uses: azure/CLI@v1
#         with:
#           inlineScript: |
#             az webapp remote-connection create \
#               --resource-group ${{ secrets.AZURE_RESOURCE_GROUP }} \
#               --name ${{ env.AZURE_WEBAPP_NAME }} \
#               --port 2222 \
#               --timeout 360 &
